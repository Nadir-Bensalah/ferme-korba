---
/**
 * Les styles du parcours d'achat : panier, commande, confirmation, suivi,
 * compte. Ils vivent ici, dans les pages du tunnel, et pas dans global.css.
 * Tout ce qui bouge s'arrête quand l'utilisateur réduit les animations.
 */
---

<style is:global>
  /* --------------------------------------------------------------
     Barre vers la livraison offerte
     -------------------------------------------------------------- */
  .fk-bar {
    position: relative;
    height: 10px;
    border-radius: 999px;
    background: var(--color-cream-2);
    overflow: visible;
  }
  .fk-bar-fill {
    position: relative;
    height: 100%;
    min-width: 10px;
    border-radius: 999px;
    overflow: hidden;
    background: linear-gradient(90deg, var(--color-yolk), var(--color-yolk-deep));
    transition:
      width 0.7s var(--ease-out-expo),
      background 0.4s ease;
  }
  .fk-bar-fill[data-full='true'] {
    background: linear-gradient(90deg, var(--color-prairie), #2f9d55);
  }
  .fk-bar-shine {
    position: absolute;
    inset-block: 0;
    inset-inline-start: -40%;
    width: 40%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.65), transparent);
    animation: fk-shine 2.6s ease-in-out infinite;
  }
  @keyframes fk-shine {
    0% {
      transform: translateX(0);
    }
    60%,
    100% {
      transform: translateX(350%);
    }
  }
  html[dir='rtl'] .fk-bar-shine {
    animation-name: fk-shine-rtl;
  }
  @keyframes fk-shine-rtl {
    0% {
      transform: translateX(0);
    }
    60%,
    100% {
      transform: translateX(-350%);
    }
  }
  /* L'éclat au moment où le seuil est atteint. */
  .fk-burst {
    position: absolute;
    inset-inline-end: -6px;
    top: 50%;
    color: var(--color-yolk);
    transform-origin: center;
    animation: fk-burst 1.1s var(--ease-spring) forwards;
  }
  @keyframes fk-burst {
    0% {
      transform: translateY(-50%) scale(0.2) rotate(-30deg);
      opacity: 0;
    }
    35% {
      transform: translateY(-50%) scale(1.15) rotate(10deg);
      opacity: 1;
    }
    100% {
      transform: translateY(-140%) scale(0.9) rotate(25deg);
      opacity: 0;
    }
  }

  /* --------------------------------------------------------------
     Compte à rebours de l'heure limite
     -------------------------------------------------------------- */
  .fk-cutoff {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    border-radius: var(--radius-md);
    border: 1px solid var(--color-line);
    background: var(--color-cream);
    padding: 0.75rem 1rem;
    min-height: 48px;
  }
  .fk-cutoff .fk-cutoff-ico {
    color: var(--color-prairie);
    flex: none;
  }
  .fk-cutoff.is-urgent {
    border-color: color-mix(in srgb, var(--color-yolk) 45%, transparent);
    background: var(--color-yolk-soft);
  }
  .fk-cutoff.is-urgent .fk-cutoff-ico {
    color: var(--color-yolk-deep);
    animation: fk-tick 2s steps(2, end) infinite;
  }
  @keyframes fk-tick {
    0%,
    100% {
      transform: rotate(-6deg);
    }
    50% {
      transform: rotate(6deg);
    }
  }
  .fk-cutoff.is-past {
    background: var(--color-paper);
  }
  .fk-strong {
    font-weight: 800;
    font-variant-numeric: tabular-nums;
  }

  /* --------------------------------------------------------------
     Résumé collant en bas d'écran (panier)
     -------------------------------------------------------------- */
  .fk-sticky {
    position: fixed;
    inset-inline: 0;
    bottom: calc(var(--fk-sticky-bottom, var(--bottom-bar-h)) + env(safe-area-inset-bottom));
    z-index: 35;
    border-top: 1px solid var(--color-line);
    background: color-mix(in srgb, var(--color-paper) 94%, transparent);
    backdrop-filter: blur(12px);
    padding: 0.625rem 1rem;
    box-shadow: 0 -8px 24px -16px rgba(22, 32, 26, 0.5);
    transition:
      transform 0.35s var(--ease-out-expo),
      opacity 0.25s ease;
  }
  .fk-sticky[data-off='true'] {
    transform: translateY(130%);
    opacity: 0;
    pointer-events: none;
  }
  @media (width >= 1024px) {
    .fk-sticky {
      display: none;
    }
  }

  /* --------------------------------------------------------------
     Suggestions du panier
     -------------------------------------------------------------- */
  .fk-suggest {
    display: flex;
    width: 15rem;
    flex: none;
    align-items: center;
    gap: 0.75rem;
    border-radius: var(--radius-md);
    border: 1px solid var(--color-line);
    background: var(--color-paper);
    padding: 0.5rem;
    transition:
      border-color 0.2s,
      transform 0.2s;
  }
  .fk-suggest:hover {
    border-color: var(--color-line-2);
    transform: translateY(-2px);
  }
  .fk-suggest-add {
    display: inline-flex;
    height: 44px;
    width: 44px;
    flex: none;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    background: var(--color-prairie-soft);
    color: var(--color-prairie-deep);
    transition:
      background 0.2s,
      transform 0.2s;
  }
  .fk-suggest-add:hover {
    background: var(--color-prairie);
    color: #fff;
  }
  .fk-suggest-add:active {
    transform: scale(0.92);
  }

  /* --------------------------------------------------------------
     États vides illustrés
     -------------------------------------------------------------- */
  .fk-empty-art {
    position: relative;
    display: grid;
    place-items: center;
    height: 6.5rem;
    width: 6.5rem;
  }
  .fk-empty-art::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 999px;
    background: var(--color-cream);
  }

  /* --------------------------------------------------------------
     Progression du tunnel de commande
     -------------------------------------------------------------- */
  .fk-steps-track {
    position: relative;
    height: 3px;
    border-radius: 999px;
    background: var(--color-line);
    overflow: hidden;
  }
  .fk-steps-track > span {
    display: block;
    height: 100%;
    border-radius: 999px;
    background: var(--color-prairie);
    transition: width 0.6s var(--ease-out-expo);
  }
  .step-link.is-ok .step-num {
    background: var(--color-prairie);
    color: #fff;
  }
  .step-link.is-ok {
    color: var(--color-prairie-deep);
  }

  /* Cartes de choix : la coche du choix retenu. */
  .fk-choice-check {
    display: inline-flex;
    height: 22px;
    width: 22px;
    flex: none;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    border: 2px solid var(--color-line-2);
    color: transparent;
    transition:
      background 0.2s,
      border-color 0.2s,
      color 0.2s;
  }
  [aria-checked='true'] .fk-choice-check {
    border-color: var(--color-prairie);
    background: var(--color-prairie);
    color: #fff;
  }

  /* --------------------------------------------------------------
     Confirmation : les trois étapes qui suivent
     -------------------------------------------------------------- */
  .fk-next {
    position: relative;
    padding-inline-start: 3.25rem;
    min-height: 3rem;
  }
  .fk-next::before {
    content: '';
    position: absolute;
    inset-inline-start: 1.24rem;
    top: 2.75rem;
    bottom: -1.25rem;
    width: 2px;
    background: var(--color-line);
  }
  .fk-next:last-child::before {
    display: none;
  }
  .fk-next-ico {
    position: absolute;
    inset-inline-start: 0;
    top: 0;
    display: inline-flex;
    height: 2.5rem;
    width: 2.5rem;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    background: var(--color-prairie-soft);
    color: var(--color-prairie-deep);
  }

  /* --------------------------------------------------------------
     Suivi : la frise
     -------------------------------------------------------------- */
  /* La pastille se posait sur la première lettre du statut : le retrait de la
     liste décale son bloc, il faut la ramener dans la marge, sur le trait. */
  .fk-timeline .timeline-dot {
    inset-inline-start: -2rem;
  }

  /* L'étape en cours respire. */
  .timeline-dot.is-current.fk-breath {
    animation: fk-breath 2.4s ease-in-out infinite;
  }
  @keyframes fk-breath {
    0%,
    100% {
      box-shadow: 0 0 0 4px var(--color-prairie-soft);
    }
    50% {
      box-shadow: 0 0 0 9px color-mix(in srgb, var(--color-prairie-soft) 55%, transparent);
    }
  }

  /* --------------------------------------------------------------
     Compte : les commandes en tickets
     -------------------------------------------------------------- */
  .fk-ticket {
    position: relative;
    border-radius: var(--radius-md);
    background: var(--color-cream);
    border: 1px solid var(--color-line);
  }
  /* La ligne pointillée porte ses deux encoches : elles restent alignées,
     quelle que soit la hauteur de l'en-tête du ticket. */
  .fk-ticket-dash {
    position: relative;
    border-top: 2px dashed var(--color-line-2);
    margin-inline: 0.75rem;
  }
  .fk-ticket-dash::before,
  .fk-ticket-dash::after {
    content: '';
    position: absolute;
    top: -11px;
    height: 20px;
    width: 20px;
    border-radius: 999px;
    background: var(--color-paper);
    border: 1px solid var(--color-line);
  }
  .fk-ticket-dash::before {
    left: -22px;
  }
  .fk-ticket-dash::after {
    right: -22px;
  }
  .fk-ticket-open {
    display: flex;
    width: 100%;
    align-items: center;
    gap: 0.75rem;
    padding: 0.875rem 1rem;
    text-align: start;
    min-height: 48px;
  }
  .fk-ticket-chevron {
    transition: transform 0.25s var(--ease-out-expo);
  }
  [aria-expanded='true'] .fk-ticket-chevron {
    transform: rotate(90deg);
  }
  html[dir='rtl'] .fk-ticket-chevron {
    transform: rotate(180deg);
  }
  html[dir='rtl'] [aria-expanded='true'] .fk-ticket-chevron {
    transform: rotate(90deg);
  }

  /* Petite entrée douce, réutilisée par les blocs qui apparaissent. */
  .fk-pop {
    animation: fk-pop 0.4s var(--ease-spring) both;
  }
  @keyframes fk-pop {
    from {
      opacity: 0;
      transform: translateY(6px);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .fk-bar-shine,
    .fk-burst,
    .fk-cutoff.is-urgent .fk-cutoff-ico,
    .timeline-dot.is-current.fk-breath,
    .fk-pop {
      animation: none;
    }
    .fk-bar-shine {
      display: none;
    }
    .fk-sticky,
    .fk-bar-fill,
    .fk-steps-track > span {
      transition: none;
    }
  }

  /* --------------------------------------------------------------
     Page panier, d'après la maquette
     -------------------------------------------------------------- */
  .fk-cutoff {
    border-radius: var(--radius-lg);
    border-color: color-mix(in srgb, var(--color-prairie) 22%, transparent);
    background: var(--color-prairie-soft);
    padding: 0.875rem 1.25rem;
    min-height: 64px;
    flex: none;
  }
  .fk-line {
    position: relative;
    display: flex;
    align-items: center;
    gap: 1rem;
    border-radius: var(--radius-lg);
    background: var(--color-paper);
    box-shadow: var(--shadow-card);
    padding: 1rem;
    padding-inline-end: 4.5rem;
  }
  @media (width >= 640px) {
    .fk-line {
      gap: 1.25rem;
      padding: 1.25rem;
      padding-inline-end: 5.5rem;
    }
  }
  .fk-line .stepper-value {
    white-space: nowrap;
  }
  .fk-line-photo {
    display: block;
    flex: none;
    width: 6rem;
    height: 5rem;
    overflow: hidden;
    border-radius: var(--radius-md);
    background: var(--color-cream);
  }
  @media (width >= 640px) {
    .fk-line-photo {
      width: 9.5rem;
      height: 6.5rem;
    }
  }
  .fk-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    border-radius: 999px;
    background: var(--color-prairie-soft);
    color: var(--color-prairie-deep);
    padding: 0.3rem 0.75rem;
    font-size: 13px;
    font-weight: 600;
  }
  .fk-trash {
    position: absolute;
    inset-inline-end: 1rem;
    top: 1rem;
    display: inline-flex;
    height: 3rem;
    width: 3rem;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    background: var(--color-cream);
    color: var(--color-ink-2);
    transition:
      background 0.2s,
      color 0.2s;
  }
  @media (width >= 640px) {
    .fk-trash {
      top: 50%;
      transform: translateY(-50%);
      inset-inline-end: 1.25rem;
    }
  }
  .fk-trash:hover {
    background: var(--color-paprika);
    color: #fff;
  }
  .fk-weight {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    border-radius: var(--radius-lg);
    background: var(--color-cream-2);
    padding: 1rem 1.25rem;
  }
  @media (width >= 1024px) {
    .fk-weight {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      gap: 2rem;
    }
  }
  .fk-forgot {
    border-radius: var(--radius-lg);
    background: var(--color-cream);
    padding: 1.25rem;
  }
  .fk-suggest {
    align-items: center;
    padding: 0.625rem;
  }
  @media (width >= 640px) {
    .fk-forgot .fk-suggest {
      width: auto;
      min-width: 0;
      gap: 0.5rem;
      padding: 0.5rem;
    }
  }
  .fk-suggest-add.is-solid {
    height: 40px;
    width: 40px;
    background: var(--color-prairie);
    color: #fff;
  }
  .fk-suggest-add.is-compact {
    height: 36px;
    width: 36px;
  }
  .fk-suggest-add.is-solid:hover {
    background: var(--color-prairie-deep);
  }
  .fk-recap {
    position: relative;
  }
  .fk-recap-card {
    position: relative;
    overflow: hidden;
    border-radius: 1.5rem;
    background: var(--color-paper);
    box-shadow: var(--shadow-card);
    padding: 1.25rem 1.125rem 1.5rem;
  }
  /* La maison en filigrane, dans le coin du titre. */
  .fk-recap-house {
    position: absolute;
    inset-inline-end: 1.125rem;
    top: 0.25rem;
    width: 14rem;
    opacity: 0.9;
    pointer-events: none;
  }
  html[dir='rtl'] .fk-recap-house {
    transform: scaleX(-1);
  }
  .fk-free {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    border-radius: var(--radius-md);
    background: var(--color-prairie-soft);
    border: 1px solid color-mix(in srgb, var(--color-prairie) 20%, transparent);
    padding: 0.875rem 1rem;
  }
  .fk-free-check {
    display: inline-flex;
    height: 28px;
    width: 28px;
    flex: none;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    background: var(--color-prairie);
    color: #fff;
  }
  /* Le nid d'œufs déborde du coin bas du récapitulatif. */
  .fk-recap-eggs {
    position: absolute;
    inset-inline-end: -2.5rem;
    bottom: -3.5rem;
    width: 13rem;
    pointer-events: none;
    filter: drop-shadow(0 10px 18px rgba(22, 32, 26, 0.18));
  }
  @media (width >= 640px) {
    .fk-recap-eggs {
      width: 16rem;
      inset-inline-end: -3.5rem;
      bottom: -4rem;
    }
  }
  html[dir='rtl'] .fk-recap-eggs {
    transform: scaleX(-1);
  }
</style>
